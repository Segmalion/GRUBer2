//---------------------------------------------------------------------------
// Шаблон для UpdateSecrets.h - реального файлу з токеном (гітігнориться,
// НЕ комітиться). Скопіюйте цей файл як UpdateSecrets.h і підставте
// справжній GitHub-токен перед збіркою релізу.
//
// Токен: fine-grained Personal Access Token, права ТІЛЬКИ
// "Contents: Read-only" на репозиторій Segmalion/GRUBer2 (і більше нічого) -
// саме він дозволяє GRUBer.exe читати /releases/latest та скачувати ассети
// приватного репозиторію. Це НЕ той самий токен, яким `tools\build_release.ps1`
// публікує релізи (той - окремий, write-scoped, живе лише на машині
// розробника в $env:GRUBER_RELEASE_TOKEN, і в жодному вигляді не потрапляє
// у прошивку програми).
//
// Порожній токен = перевірка оновлень тихо вимкнена (без діалогів і падінь) -
// див. Update_FetchLatestRelease() в UpdateCheck.cpp.
#pragma once
#define GITHUB_UPDATE_TOKEN L""
